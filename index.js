function takeANumer(line, person) {
  line = [...line, person];
  var personIndex = line.findIndex(person);
  return `Welcome, ${person}. You are number ${personIndex} in line.`;
}

function nowServing() {
  if ()
}

function currentLine(line) {
  if (line === []) {
    return "The line is currently empty.";
  } else {
    return 
  }
}